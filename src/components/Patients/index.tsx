import { Typography } from "@mui/material";

const Patients = () => {
  return (
    <Typography variant="h4" gutterBottom>
      Профиль
    </Typography>
  );
};

export default Patients;
